{
  "name": "things-to-do-api",
  "version": "1.0.0",
  "description": "A simple JSON mock API of to do things",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api": "json-server --watch ./src/db.json"
  },
  "author": "Amanda",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
